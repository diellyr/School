/**
 * Fonte única da versão exibida na tela de login e na barra lateral — mantida em sincronia com
 * `package.json` e com a entrada mais recente de `version.md` a cada mudança relevante.
 */
export const APP_VERSION = '0.9.1';
